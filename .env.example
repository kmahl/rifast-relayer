# ===============================================
# RIFAST RELAYER SERVICE - ENVIRONMENT VARIABLES
# ===============================================

# 🔒 CRITICAL: ADMIN PRIVATE KEY (ONLY SERVICE THAT HOLDS THIS)
# This key has owner permissions on RifasPlatform contract
# NEVER commit this to git, NEVER share, NEVER expose to backend
ADMIN_PRIVATE_KEY=0x...

# 🔐 RELAYER API KEY
# Shared secret between backend and relayer
# Backend must include this in X-API-Key header
# Generate: openssl rand -hex 32
RELAYER_API_KEY=your-secure-random-api-key-here

# 🌐 BLOCKCHAIN RPC
# BSC Testnet: https://bsc-testnet-rpc.publicnode.com
# BSC Mainnet: https://bsc-dataseed.binance.org
RPC_URL=http://127.0.0.1:8545

# 📍 CONTRACT ADDRESS
# RifasPlatform contract address (chain-specific)
CONTRACT_ADDRESS=0x9fE46736679d2D9a65F0992F2272dE9f3c7fa6e0

# 🌍 ENVIRONMENT
NODE_ENV=development

# 🔧 SERVER CONFIG
PORT=3002
HOST=0.0.0.0

# 🛡️ SECURITY
# IP whitelist: comma-separated IPs allowed to call relayer
# Example: 127.0.0.1,192.168.1.100
# Leave empty to allow all (NOT recommended for production)
ALLOWED_IPS=

# Rate limit: max requests per minute per IP
RATE_LIMIT_PER_MINUTE=10

# 📊 LOGGING
LOG_LEVEL=info
